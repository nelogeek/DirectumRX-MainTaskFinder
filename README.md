# 🔍 DirectumRX MainTask Finder

Консольное приложение для поиска корневой задачи (MainTask) в системе электронного документооборота **DirectumRX** по ссылке на задание/задачу. Автоматически проходит цепочку связанных заданий и задач через таблицы `sungero_wf_assignment` и `sungero_wf_task`.

## 📌 Назначение

Приложение решает типичную проблему администраторов DirectumRX: когда задание «зависает» или ведёт себя некорректно, требуется найти его корневую задачу (поле `MainTask`), чтобы понять контекст выполнения бизнес-процесса. Вручную это делать долго — приложение автоматизирует поиск за секунды.

## ✨ Ключевые возможности

- 🔒 **Безопасное хранение учётных данных** — пароли шифруются через Windows DPAPI и доступны только текущему пользователю на этом ПК
- ⚡ **Однократная авторизация** — после первого подключения достаточно нажать Enter для повторного запуска
- 🔄 **Умные дефолты** — при изменении параметров подключения новые значения подставляются из последней сессии
- 🌐 **SSH-туннелирование** — поддержка подключения к БД через SSH-бастеон
- 🆓 **Автоподбор порта** — избегает конфликтов при занятых локальных портах (диапазон 54321–54400)
- 🧹 **Очистка сессии** — команда `очистить` удаляет локальные учётные данные

## ⚙️ Требования

| Компонент | Версия |
|-----------|--------|
| Операционная система | Windows 10/11 (требуется DPAPI для шифрования) |
| .NET Runtime | .NET 8.0 или новее |
| База данных | PostgreSQL с установленным решением **Sungero** (модуль рабочих процессов DirectumRX) |
| Доступ | Учётная запись с правами SELECT к таблицам `sungero_wf_assignment`, `sungero_wf_task` |

> 💡 Приложение **не требует** установки — достаточно собрать проект или запустить через `dotnet run`.

## 🚀 Быстрый старт

### 1. Клонирование и сборка

```bash
git clone https://github.com/nelogeek/MainTaskFinder.git
cd MainTaskFinder
dotnet build
```

### 2. Запуск
```bash
dotnet run
```

### 3. Первое подключение

При первом запуске будет запрошена информация для подключения:
```bash
Подключаться через SSH? [да]: 
SSH хост [10.12.132.29]: 
SSH порт [22]: 
SSH пользователь [emsedsystem]: 
SSH пароль [*************]: •••••••••••••
Хост БД [10.12.132.14]: 
Порт БД [5432]: 
Имя БД [directumrx]: 
Пользователь БД [userrx]: 
Пароль БД [*************]: •••••••••••••
```

>💡 Пароли вводятся без эха (символы заменяются на *). Для использования дефолтных значений просто нажмите Enter.

### 4. Поиск корневой задачи
После успешного подключения введите ссылку на задание из веб-интерфейса DirectumRX:

```bash
Введите ссылку на задачу/задание (или 'выход' для завершения):
> https://sed.ru/DrxWeb/#/sat/card/90daecb7-5d5d-465e-95a4-3235b8c01d5b/67572604
```

Приложение автоматически:
1. Извлечёт ID (67572604) и Discriminator (90daecb7-5d5d-465e-95a4-3235b8c01d5b)
2. Пройдёт цепочку связей task → ParentTask / ParentAsg
3. Найдёт запись, где Id = MainTask, и выведет результат:

```bash
✅ Найдена корневая задача! MainTask = 67572590
```


## 🔐 Безопасность

- Учётные данные никогда не хранятся в открытом виде
- Шифрование выполняется через ProtectedData (Windows DPAPI)
- Расшифровать файл session/credentials.dat может только тот же пользователь Windows на этом же компьютере
- Файл сессии не должен попадать в Git — добавьте в .gitignore:

```gitignore
# Локальные сессии подключения
session/
*.dat
```

## 🧹 Управление сессией

| Действие | Как сделать|
|-----------|--------|
|Использовать последнюю сессию | При запуске нажмите Enter на вопросе «Подключиться к последней сессии?» |
| Изменить параметры подключения | Введите нет при вопросе о сессии → отредактируйте нужные поля |
|Очистить сохранённые данные | В поле ввода ссылки введите команду: очистить |


## ⚠️ Важные замечания
1. Только для внутреннего использования — приложение предназначено для администрирования вашей корпоративной системы DirectumRX. Не используйте его в публичных средах.
2. Требуются права на чтение БД — учётная запись БД должна иметь права SELECT к таблицам рабочих процессов.
3. Работает только на Windows — шифрование через DPAPI недоступно на Linux/macOS.
4. Не модифицирует данные — приложение выполняет только SELECT-запросы, изменения в БД невозможны.



